{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/chroma/chroma.go",
            "cwd": "${workspaceFolder}",
            "env": {},
            "args": [
                "do", "ex", "smartgestures", "--pkgbuild", "~/Projects/cyberlinux/aur/chromium/PKGBUILD"
            ]
        }
    ]
}
